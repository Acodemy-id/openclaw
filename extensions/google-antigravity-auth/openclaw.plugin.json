{
  "id": "google-antigravity-auth",
  "providers": [
    "google-antigravity"
  ],
  "configSchema": {
    "type": "object",
    "additionalProperties": true,
    "properties": {
      "rotationStrategy": {
        "type": "string",
        "enum": [
          "round-robin",
          "priority",
          "least-used",
          "failover"
        ],
        "default": "round-robin"
      }
    }
  }
}