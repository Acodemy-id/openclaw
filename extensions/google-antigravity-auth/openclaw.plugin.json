{
  "id": "google-antigravity-auth",
  "providers": [
    "google-antigravity"
  ],
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "rotationStrategy": {
        "type": "string",
        "enum": [
          "round-robin",
          "priority",
          "least-used",
          "failover"
        ],
        "default": "round-robin"
      }
    }
  }
}